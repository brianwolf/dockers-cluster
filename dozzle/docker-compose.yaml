version: "3.8"

services:
  dozzle:
    image: amir20/dozzle:v6.6.3
    container_name: dozzle
    hostname: dozzle
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro

    expose:
      - 8080
