name: docker-sync

on:
  push:
    branches:
      - main
      
jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
    - name: connect with docker-sync
      run: curl --connect-timeout 5 http://pc.brianlobo.theworkpc.com:5000/api/v1/sync/
  